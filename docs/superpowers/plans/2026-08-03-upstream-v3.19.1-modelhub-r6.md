# Upstream v3.19.1 ModelHub R6 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 fork 当前 R5 主线与 `farion1231/cc-switch` 最新 `main` 合并，保留 ModelHub 兼容与安装器能力，重新构建 macOS arm64 应用并发布 R6 Latest Release 和 Draft PR。

**Architecture:** 从已合并 R5 的 `origin/main` 创建发布分支，以普通 merge commit 引入 `upstream/main`，只在冲突点组合双方行为。随后把安装器固定 tag 与 App 资产名升级到 R6/3.19.1，使用合并后源码构建 ad-hoc signed `.app`，再由现有 allowlist packager 生成四项发布资产并从 GitHub 回下载复验。

**Tech Stack:** Git/GitHub CLI、pnpm/Vite/Vitest、Rust 1.95/Cargo/Tauri 2、macOS `codesign`/`ditto`、Bash 3.2、SQLite 3 JSON1、GitHub Release。

## Global Constraints

- Fork 基线固定为 `origin/main@a0ff6b0ee8c21c4389df404f501225f46116e371`，其中已合并 R5 PR #4。
- 上游输入固定为本轮 fetch 得到的 `upstream/main@f38722a440b2bda3df10929758e296f2cbcd96de`；发布前若远端继续前进，只报告本轮固定 SHA，不静默改变输入。
- 新分支固定为 `feat/upstream-v3.19.1-modelhub-r6`，PR base 为 fork 的 `main`；只允许普通 push，不强推。
- 必须保留 fork 的 ModelHub 代理兼容、AK 三方一致性、事务回滚、一键安装器与完整配置快照行为。
- 合并后的应用版本为上游 `3.19.1`，安装器 tag 固定为 `modelhub-installer-20260803-r6`，App 资产名固定为 `CC-Switch-ModelHub-3.19.1-arm64.app.zip`。
- R1-R5 Release 与资产全部保留；R6 发布为非 Draft、非 Pre-release 的 Latest。
- Release 不得包含真实 AK/OAuth、`auth.json`、日志、请求/会话/用量记录、备份或 `/Users/shopee` 用户配置路径。
- 用户入口保持 `https://github.com/lixinyao0722/cc-switch/releases/latest/download/install.sh`，且整条安装管道不得使用 `sudo`。

---

### Task 1: 建立分支并合并上游

**Files:**
- Merge: `origin/main` + `upstream/main`
- Resolve: `src-tauri/src/proxy/session.rs`
- Verify merged fork-only paths: `src-tauri/src/proxy/modelhub_compat.rs`, `src-tauri/src/proxy/retry_429.rs`, `scripts/modelhub-installer/**`, `tests/scripts/modelhub-installer.test.sh`

**Interfaces:**
- Consumes: fork R5 主线、上游 3.19.1 后续 `main`。
- Produces: 同时包含上游 45 个 fork 缺失提交与 fork ModelHub 提交的 merge commit。

- [ ] **Step 1: 从最新 fork 主线创建分支**

```bash
git switch -c feat/upstream-v3.19.1-modelhub-r6 origin/main
git status -sb
```

Expected: named branch，工作区除本计划外无其他改动。

- [ ] **Step 2: 记录合并前基线验证**

```bash
pnpm install --frozen-lockfile
pnpm test:installer
pnpm typecheck
pnpm test:unit
```

Expected: R5 installer、前端类型检查和单测均退出 0；任何既有失败先记录，不归因于上游合并。

- [ ] **Step 3: 合并固定上游 SHA**

```bash
git merge --no-ff f38722a440b2bda3df10929758e296f2cbcd96de \
  -m "chore(upstream): 合并 CC Switch 最新更新"
```

Expected: 仅 `src-tauri/src/proxy/session.rs` 需要人工冲突处理。

- [ ] **Step 4: 组合冲突两侧行为并提交 merge**

保留 fork 的 ModelHub session header/compatibility 行为，同时保留上游 parent rollout timeline cache 与 3.19.1 session 处理；运行该模块相关 Rust tests 后完成 merge commit。

```bash
git add src-tauri/src/proxy/session.rs
git commit --no-edit
git diff --check origin/main...HEAD
```

Expected: merge 完成，无冲突标记或 whitespace error，fork-only 文件仍存在。

---

### Task 2: 升级 R6 固定版本契约

**Files:**
- Modify: `scripts/modelhub-installer/install.sh`
- Modify: `scripts/modelhub-installer/package-release.sh`
- Modify: `tests/scripts/modelhub-installer.test.sh`
- Modify: `docs/guides/modelhub-codex-proxy-compat-zh.md`
- Add: `docs/superpowers/plans/2026-08-03-upstream-v3.19.1-modelhub-r6.md`

**Interfaces:**
- Produces: immutable R6 tag 与 `CC-Switch-ModelHub-3.19.1-arm64.app.zip` 四资产契约。

- [ ] **Step 1: 先更新测试期望并验证 RED**

将安装器 tag 期望改为 `modelhub-installer-20260803-r6`，App 资产期望改为 `CC-Switch-ModelHub-3.19.1-arm64.app.zip`。

```bash
pnpm test:installer -- 'preflight downloads from immutable release tag'
pnpm test:installer -- 'package builds exact allowlisted release assets'
```

Expected: 生产常量仍为 R5/3.18.0，两个定向检查失败。

- [ ] **Step 2: 更新生产常量和用户说明**

同步 `install.sh` 的 `RELEASE_TAG`/`APP_ASSET`、`package-release.sh` 的 `OUTPUT_APP_NAME`，并在中文指南记录 R6 合并上游 3.19.1 与安装入口不变。

- [ ] **Step 3: 验证 GREEN 并提交版本契约**

```bash
/bin/bash -n scripts/modelhub-installer/install.sh
/bin/bash -n scripts/modelhub-installer/package-release.sh
plutil -lint scripts/modelhub-installer/templates/com.ccswitch.modelhub-env.plist
pnpm test:installer
git diff --check
git add docs/guides/modelhub-codex-proxy-compat-zh.md \
  docs/superpowers/plans/2026-08-03-upstream-v3.19.1-modelhub-r6.md \
  scripts/modelhub-installer/install.sh \
  scripts/modelhub-installer/package-release.sh \
  tests/scripts/modelhub-installer.test.sh
git commit -m "build(installer): 发布上游 3.19.1 R6 资产"
```

Expected: installer 全量测试退出 0，提交范围仅包含 R6 契约、指南与计划。

---

### Task 3: 完整验证、构建和本地资产验收

**Files:**
- Generated app: `src-tauri/target/release/bundle/macos/CC Switch.app`
- Generated ignored assets: `release/modelhub-installer-r6/`
- Portable snapshot: temporary directory from `mktemp -d`

**Interfaces:**
- Consumes: 合并后 3.19.1 源码、当前本机脱敏配置快照。
- Produces: R6 的 `install.sh`、3.19.1 App ZIP、resources tarball 与 checksum manifest。

- [ ] **Step 1: 完整源码验证**

```bash
pnpm install --frozen-lockfile
pnpm typecheck
pnpm format:check
pnpm test:unit
pnpm test:installer
cd src-tauri
LZMA_API_STATIC=1 cargo fmt --check
LZMA_API_STATIC=1 cargo clippy --all-targets -- -D warnings
LZMA_API_STATIC=1 cargo test
```

Expected: 所有命令使用 Rust 1.95 toolchain 并退出 0。

- [ ] **Step 2: 构建 3.19.1 arm64 App**

```bash
LZMA_API_STATIC=1 pnpm tauri build \
  --bundles app \
  --config '{"bundle":{"createUpdaterArtifacts":false}}'
/usr/bin/codesign --verify --deep --strict --verbose=2 \
  'src-tauri/target/release/bundle/macos/CC Switch.app'
/usr/bin/lipo -archs \
  'src-tauri/target/release/bundle/macos/CC Switch.app/Contents/MacOS/cc-switch'
```

Expected: bundle version `3.19.1`、架构 `arm64`、strict codesign 通过。

- [ ] **Step 3: 生成 portable golden 与四资产**

用 `build-local-golden-snapshot.sh` 从当前本机 Codex/CC Switch 配置生成临时脱敏快照；用 `ditto -c -k --sequesterRsrc --keepParent` 生成固定 App ZIP；设置 `CC_SWITCH_GOLDEN_SNAPSHOT_DIR` 调用 `package-release.sh` 输出到 `release/modelhub-installer-r6/publish`。

- [ ] **Step 4: 验收发布字节**

```bash
(cd release/modelhub-installer-r6/publish && shasum -a 256 -c SHA256SUMS.txt)
CC_SWITCH_RELEASE_SMOKE_ASSET_DIR="$PWD/release/modelhub-installer-r6/publish" \
  pnpm test:installer -- release-smoke
```

同时验证资源 allowlist、SQLite `integrity_check=ok`、Provider auth 为空、日志/会话/用量表为空、helper/App 签名与 arm64 架构，以及 installer/resources/portable golden 对真实凭据和 `/Users/shopee` 零命中。

---

### Task 4: 推送、创建 Draft PR 并发布 R6

**Files:**
- No additional repository changes expected
- Temporary PR body and release notes: private `mktemp -d` directory

**Interfaces:**
- Produces: remote branch、Draft PR、Latest R6 Release。

- [ ] **Step 1: 发布前状态门禁**

```bash
git status --short --branch
git diff --check origin/main...HEAD
git log --oneline origin/main..HEAD
gh auth status
```

Expected: tracked tree 干净、分支为 `feat/upstream-v3.19.1-modelhub-r6`，验证证据对应当前 HEAD。

- [ ] **Step 2: 普通 push 并创建 Draft PR**

```bash
git push -u origin feat/upstream-v3.19.1-modelhub-r6
```

PR 标题固定为 `chore(upstream): 合并 3.19.1 更新并发布 ModelHub R6`，正文说明上游更新摘要、唯一冲突处理、fork 能力保留、验证命令、Release 资产和回滚入口。

- [ ] **Step 3: 创建 Draft Release 并核对 digest**

创建 `modelhub-installer-20260803-r6` Draft Release，target 精确为当前 HEAD，上传四资产；比较 GitHub 返回的 asset name/size/digest 与本地 SHA，不一致则保持 Draft 并停止。

- [ ] **Step 4: 发布 Latest 并从 GitHub 回下载复验**

将 R6 改为 non-draft、non-prerelease、Latest。下载到新的临时目录，重新执行 checksum、archive allowlist、敏感扫描、App/helper 签名与 `release-smoke`；确认 `/releases/latest/download/install.sh` 最终指向 R6。

## Self-Review

- Spec coverage: 上游合并、更新摘要、R6 版本契约、源码构建、四资产、Draft PR、正式 Latest 与回下载验证均有对应任务。
- Placeholder scan: 无 `TBD`、`TODO`、未定义版本号或未指定验证命令。
- Interface consistency: 分支、upstream SHA、app 版本、tag、资产名与 PR base 在全部任务中一致。
